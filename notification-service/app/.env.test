# Database Configuration
DB_USER=user
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=3306
DB_NAME=golfclub

# Celery Configuration with MQTT
CELERY_BROKER_URL=mqtt://localhost:1883
CELERY_RESULT_BACKEND=cache

# MQTT Configuration
MQTT_BROKER_URL=localhost
MQTT_BROKER_PORT=1883
MQTT_USERNAME=test_mqtt_user
MQTT_PASSWORD=test_mqtt_password
MQTT_KEEPALIVE=60
MQTT_TLS_ENABLED=False

# Notification Service Configuration
NOTIFICATION_CHECK_INTERVAL=10
MAX_RETRIES=2
RETRY_DELAY=60

# External Service Configurations (Mock values for testing)
SMTP_SERVER=mock_smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=test_username
SMTP_PASSWORD=test_password
PUSH_NOTIFICATION_API_KEY=test_api_key_12345

# Logging Configuration
LOG_LEVEL=DEBUG
LOG_FILE=test_notification_service.log

# API Configuration
API_SECRET_KEY=test_secret_key_67890
API_RATE_LIMIT=1000 per minute

# Set the environment to testing
FLASK_ENV=testing